import {StyleSheet} from 'react-native'
import {Metrics} from '../../../Themes'

const styles = StyleSheet.create({
    marginItem: {
        margin: Metrics.MARGIN_LV1
    }
})

export default styles