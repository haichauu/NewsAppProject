import React, { Component } from 'react';
import { View, Text } from 'react-native';
import AppText from '../Common/AppText'

const NavigationBar = () => {
    const {text, fetching} = props;
  return (
      <View>
        <AppText />
      </View>
    );
  
}
